html(lang="en")
    head
        title SCOTT.emp CRUD
        meta(charset="UTF-8")
        script(src="/public/js/empInsert.js", defer=true)
            //html은 defer 만 쓴다.
            //https://ko.javascript.info/onload-ondomcontentloaded 참고
    body
        h1 📁 사원 등록 폼
        form(id="empInsertForm",action="/empInsert.do",method="POST")
            p
                span 사번:
                input(value="",name="empno")
                span#empnoMsg
                    //id주는법.
            p
                span 이름:
                input(value="",name="ename")
            p
                span 직책:
                input(value="",name="job")
            p
                span 급여:
                input(value="",name="sal")
            p
                span 상여금:
                input(value="",name="comm")
            p
                span 입사일: mysql.now()

            p
                span 상사:
                input(value="", name="mgr")
                span#MgrMsg
                    //empnoCheck.do로 참조 가능한 상사 구현
            p
                span 부서:
                input(value="", name="deptno")
                span#deptnoMsg
                //deptnoCheck.do를 구현하여 참조가능한 부서번호
            p
                button(type="submit") 제출
    //스크립트 쓰는법
    //script.
        //script. 하위 요소로 인지하지 않겠다.
        console.log(empInsertForm.empno);
